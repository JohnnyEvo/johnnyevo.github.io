<script>
  import { animate, stagger } from "motion";

  let nameSplitting = document.getElementsByClassName("chars");

  // Subtle entrance animation instead of infinite loop
  animate(
    nameSplitting,
    { 
      opacity: [0, 1],
      transform: ["translateY(20px)", "translateY(0px)"]
    },
    { delay: stagger(0.05), duration: 0.8, easing: "ease-out" }
  );
</script>

<h1 class="text-5xl md:text-8xl font-title font-black uppercase tracking-tighter text-text leading-[0.9]">
  <span class="block">
    <span class="not-sr-only chars inline-block">J</span>
    <span class="not-sr-only chars inline-block">o</span>
    <span class="not-sr-only chars inline-block">h</span>
    <span class="not-sr-only chars inline-block">n</span>
    <span class="not-sr-only chars inline-block">n</span>
    <span class="not-sr-only chars inline-block">y</span>
  </span>
  <span class="block text-accent">
    <span class="not-sr-only chars inline-block">C</span>
    <span class="not-sr-only chars inline-block">h</span>
    <span class="not-sr-only chars inline-block">a</span>
    <span class="not-sr-only chars inline-block">r</span>
    <span class="not-sr-only chars inline-block">c</span>
    <span class="not-sr-only chars inline-block">o</span>
    <span class="not-sr-only chars inline-block">s</span>
    <span class="not-sr-only chars inline-block">s</span>
    <span class="not-sr-only chars inline-block">e</span>
    <span class="not-sr-only chars inline-block">t</span>
  </span>
  <span class="sr-only">Johnny Charcosset</span>
</h1>
